name: tekartik_camera_web
description: Camera web utilities
version: 0.2.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  tekartik_browser_utils:
    git:
      url: https://github.com/tekartik/browser_utils.dart
      ref: dart3a
  web:
dev_dependencies:
  build_runner: '>=1.6.0'
  build_web_compilers: '>=2.3.0'
  build_test: '>=1.3.7'
  process_run:
  test:
  dev_build: '>=0.13.4+4'
