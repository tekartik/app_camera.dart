name: tekartik_app_camera_support
description: App camera support
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  process_run: ">=1.2.1+1"

dev_dependencies:
  test: ">=1.24.0"
  dev_build: ">=1.1.2"
  tekartik_common_utils:
    git:
      url: https://github.com/tekartik/common_utils.dart
      ref: dart3a
  args: ">=2.6.0"
  path: ">=1.9.0"
